<!DOCTYPE html>
<html>
  <head>
    <title>Register</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/1000hz-bootstrap-validator/0.9.0/validator.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" />
  </head>
  <body>
    <div class="login-container">
      <% if (typeof(e) !== 'undefined') { %>
        <div id='error'> <%= e %> </div>
      <% } %>
      <h2> Register </h2>
      <p> Have an account? <a href='/users/login'>Log In</a> <p>
      <form data-toggle="validator" role="form" method="post" action="/users">
        <div class="form-group">
          <label for="kerberos">MIT email:</label>
          <input type="text" class="form-control" pattern="^[a-zA-Z0-9._%+-]+@mit\.edu$" data-maxlength="16" name="kerberos" placeholder="kerberos@mit.edu" required>
        </div>
        <div class="form-group">
          <label for="password">Password:</label>
          <input type="password" data-minlength="8" class="form-control" id="password" name="password" placeholder="Enter password" required>
          <span class="help-block">Minimum of 8 characters</span>
        </div>
        <div class="form-group">
          <label for="confirm_password">Confirm Password:</label>
          <input type="password" class="form-control" name="confirm_password" placeholder="Confirm password" id="confirm-pw" data-match="#password" data-match-error="Passwords don't match" required>
          <div class="help-block with-errors"></div>
        </div>
         <button type="submit" class="btn btn-default">Register</button>
         <input type="hidden" name="_csrf" value=<%= csrf %>></input>
     </form>
    </div>
  </body>
</html>
